# Typechecking

Basic typechecking doesn't seem that hard -- I haven't actually implemented chapter 10 of _Writing a C Compiler_ yet, but it seems like each `Exp` just has a `type` property that's propagated up from constants. It seems harder to do pattern matching though.
