# Rust's `directories` crate

Different OSes store program-specific files like cached data in different places. `directories` is a simple way to get that system-specific directory given some information.
